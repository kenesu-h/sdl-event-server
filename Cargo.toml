[package]
name = "sdl-event-server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crossbeam-channel = "0.5"
sdl2 = "0.35"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
